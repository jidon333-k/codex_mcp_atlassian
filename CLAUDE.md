# Claude Instructions

Use Claude-specific project instructions from:

- `agents/claude/confluence_publisher.md`

Use shared publish runner:

- `bash scripts/publish.sh --dry-run --glob "docs/**/*.md"`
- `bash scripts/publish.sh --glob "docs/**/*.md"`
